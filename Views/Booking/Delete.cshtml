@model EventEaseApp.Models.Booking
@{
ViewData["Title"] = "Delete Booking";
}

<h2>@ViewData["Title"]</h2>

<div>
	<h4>Are you sure you want to delete this Booking</h4>
	<dl> class="row"><
		<dt class="col-sm-2">BookingId</dt>
		<dd class="col-sm-10">@Model.BookingId</dd>
		
		<dt class="col-sm-2">BookingId</dt>
		<dd class="col-sm-10">@Model.VenueId</dd>

		<dt class="col-sm-2">BookingId</dt>
		<dd class="col-sm-10">@Model.EventId</dd>

		<dt class="col-sm-2">BookingId</dt>
		<dd class="col-sm-10">@Model.BookingDate</dd>
	</dl>

	<form method="post">
		<button type="submit" class="btn btn-danger">Delete</button>
		<a href="@Url.Action("Index","Booking")" class="btn btn-secondary">Cancel</a>
	</form>
</div>
